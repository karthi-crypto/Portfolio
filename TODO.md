# TODO - Portfolio Upgrade

- [x] Step 1: Update `index.html` — add loader overlay, custom cursor, scanline/grid overlays, HUD corners + system hero markup.

- [x] Step 2: Update `style.css` — merge neon/glass/system styles, add reveal + scanline + loader + cursor styling.
- [x] Step 3: Update `script.js` — implement loader hide, custom cursor behavior, IntersectionObserver reveals, and guard mobile.

- [ ] Step 4: Verify build by opening `index.html` in browser (manual) and check: nav, canvases resize, loader/cursor, contact form submit.

